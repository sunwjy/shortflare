{
  "name": "shortflare",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "turbo run build",
    "check": "pnpm lint && pnpm check:deprecated && pnpm format:check && pnpm typecheck && pnpm test && pnpm build",
    "check:deprecated": "node packages/tooling/scripts/check-deprecated-apis.mjs apps/management/tsconfig.json apps/redirect-worker/tsconfig.json packages/analytics/tsconfig.json packages/database/tsconfig.json packages/deploy/tsconfig.json packages/links/tsconfig.json",
    "dev": "turbo run dev --parallel",
    "format": "oxfmt .",
    "format:check": "oxfmt --check .",
    "lint": "oxlint .",
    "lint:fix": "oxlint --fix .",
    "test": "turbo run test",
    "test:watch": "turbo run test:watch --parallel",
    "typecheck": "turbo run typecheck"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "oxfmt": "catalog:",
    "oxlint": "catalog:",
    "turbo": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "packageManager": "pnpm@11.15.0"
}
